<div id="recently_visited" class="item">
	<% if logged_in? %>
		<b>Recently Visited:</b><br/>
		<ul>		
			<%= render :partial => 'users/conversation', :collection => current_user.recent_conversations %> 
		</ul>
	<% end %>
</div>
