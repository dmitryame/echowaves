<div id="header">
  <div id="header_padding" class="container">
    <div class="span-16 colborder">
	  	<h1><%= link_to image_tag("echowaves.gif", :border => 0, :width => "282px", :height => "50px" ), home_path %></h1>
	  </div>
	  
    <div class="span-6 last">
      <% if logged_in? %>				
	  		<%= link_to image_tag(current_user.gravatar_url, :border => 0, :wigth => 40, :height => 40, :class => "avatar", :style => 'float:left;margin-right:4px;'), 
	  				conversation_messages_path(current_user.personal_conversation) %>
	  		<%=current_user.login %><br/>
	  		<%= link_to "edit profile", edit_user_path(current_user)  %>
	  		<%= link_to t("ui.logout"), logout_path %>
	  	<%else%>			
	  		<%= link_to t("ui.login"), new_session_path %>
	  	<%end%>
	  </div>
  </div>
</div>