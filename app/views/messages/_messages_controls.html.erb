<div id="messages_controls" class="item">
	<%= link_to t('ui.back'), conversations_path %>

	<%if @has_more_messages%>
		<%= link_to_remote t("ui.more_messages"), 
		:url => {:action => 'get_more_messages', :conversation_id => @conversation},
		:with => "'before='+#{@messages.first.id}" %>
	<%end%>
</div>
