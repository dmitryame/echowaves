<div class="item2 page">
    <%= link_to image_tag(user.gravatar_url, :border => 0, :width => 40, :height => 40, :class => "avatar", :alt => user.login, :style => 'float:left;margin-right:4px;'),
 	user_path(user),
	:class => 'tip', 
	:title =>  t("users.since") + user.created_at.to_s(:pretty)  + 
	'<br/>' + pluralize(user.conversations.size, 'convo') +
	'<br/>' + pluralize(user.messages.size, 'message') +
	'<br/>' + pluralize(user.subscriptions.size, 'subscription')%>
	  <%= link_to h(user.login), conversation_messages_path(user.personal_conversation)	, 
		:class => 'tip', 
		:title => "personal convo for " + user.login %><br/>
		<br/>
</div>

